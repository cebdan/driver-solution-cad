# Подробная инструкция по установке xtd и запуску GUI

## 📋 Содержание

1. [Текущий статус](#текущий-статус)
2. [Установка зависимостей](#установка-зависимостей)
3. [Сборка xtd](#сборка-xtd)
4. [Установка xtd](#установка-xtd)
5. [Сборка GUI приложения](#сборка-gui-приложения)
6. [Запуск GUI](#запуск-gui)
7. [Альтернатива: консольная версия](#альтернатива-консольная-версия)
8. [Решение проблем](#решение-проблем)

---

## ✅ Текущий статус

### Что уже готово:
- ✅ **xtd собран на 100%** - все библиотеки скомпилированы
- ✅ **wxWidgets установлен** - через Homebrew
- ✅ **GUI код готов** - `examples/simple_gui.cpp`
- ✅ **Консольное меню работает** - `./demo_menu_console`

### Что нужно сделать:
- ⚠️ Установить xtd в систему (требуется sudo)
- ⚠️ Собрать GUI приложение
- ⚠️ Запустить GUI

---

## 🔧 Установка зависимостей

### Шаг 1: Проверка Homebrew

```bash
which brew
```

Если Homebrew не установлен, установите его:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Шаг 2: Установка wxWidgets

```bash
brew install wxwidgets
```

Проверка установки:
```bash
wx-config --version
```

Должно показать версию (например, 3.3.1)

### Шаг 3: Установка CMake (если не установлен)

```bash
brew install cmake
```

Проверка:
```bash
cmake --version
```

---

## 🏗️ Сборка xtd

### Шаг 1: Переход в директорию xtd

```bash
cd /Users/user/Documents/xtd
```

### Шаг 2: Очистка предыдущей сборки (если нужно)

```bash
rm -rf build
```

### Шаг 3: Создание директории для сборки

```bash
mkdir -p build
cd build
```

### Шаг 4: Конфигурация CMake

```bash
cmake .. \
  -DXTD_BUILD_TESTS=OFF \
  -DXTD_BUILD_EXAMPLES=OFF \
  -DXTD_BUILD_DOCUMENTATION=OFF
```

**Ожидаемый результат:**
```
-- Configuring done
-- Generating done
-- Build files have been written to: /Users/user/Documents/xtd/build
```

### Шаг 5: Сборка xtd

```bash
cmake --build . -j$(sysctl -n hw.ncpu)
```

**Время сборки:** 5-15 минут (зависит от процессора)

**Ожидаемый результат:**
```
[100%] Built target xtdc-gui
```

### Шаг 6: Проверка сборки

```bash
ls -la src/xtd.core/libxtd.core.a
ls -la src/xtd.forms/libxtd.forms.a
```

Оба файла должны существовать.

---

## 📦 Установка xtd

### Шаг 1: Установка в систему

```bash
cd /Users/user/Documents/xtd/build
sudo cmake --install .
```

**Внимание:** Потребуется ввести пароль администратора.

**Что устанавливается:**
- Заголовочные файлы в `/usr/local/include/xtd/`
- Библиотеки в `/usr/local/lib/`
- CMake конфигурация в `/usr/local/lib/cmake/xtd/`

### Шаг 2: Проверка установки

```bash
ls /usr/local/include/xtd/xtd.h
ls /usr/local/lib/libxtd.core.a
ls /usr/local/lib/cmake/xtd/xtdConfig.cmake
```

Все три команды должны показать существующие файлы.

---

## 🎨 Сборка GUI приложения

### Шаг 1: Переход в проект

```bash
cd /Users/user/Documents/driver-solution-cad
```

### Шаг 2: Создание директории для сборки

```bash
mkdir -p build
cd build
```

### Шаг 3: Конфигурация CMake

```bash
cmake ..
```

**Ожидаемый результат:**
```
-- Found xtd: /usr/local/lib/cmake/xtd/xtdConfig.cmake
-- Configuring done
-- Generating done
```

### Шаг 4: Сборка проекта

```bash
cmake --build . -j$(sysctl -n hw.ncpu)
```

**Ожидаемый результат:**
```
[100%] Built target simple_gui
```

### Шаг 5: Проверка сборки

```bash
ls -la simple_gui
```

Файл должен существовать и быть исполняемым.

---

## 🚀 Запуск GUI

### Способ 1: Прямой запуск

```bash
cd /Users/user/Documents/driver-solution-cad/build
./simple_gui
```

### Способ 2: Запуск из корня проекта

```bash
cd /Users/user/Documents/driver-solution-cad
./build/simple_gui
```

### Что вы увидите:

1. **Главное окно** с заголовком "CAD System - Main Window"
2. **Меню вверху:**
   - File (Файл)
   - Edit (Правка)
   - View (Вид)
   - Tools (Инструменты)
   - Help (Справка)
3. **Область приложения** с приветственным сообщением
4. **Строка состояния** внизу

### Использование меню:

**File → New 2D Document (Ctrl+N):**
- Создает новый 2D документ
- Показывает сообщение об успешном создании
- Устанавливает глобальную систему координат (0,0,0,0,0,0)

**File → Open... (Ctrl+O):**
- Открывает диалог выбора файла
- Поддерживает форматы: *.json, *.*

**File → Save (Ctrl+S):**
- Сохраняет текущий документ

**File → Save As... (Ctrl+Shift+S):**
- Сохраняет документ с новым именем

**File → Exit (Ctrl+Q):**
- Закрывает приложение

---

## 💻 Альтернатива: консольная версия

Если GUI не нужен или есть проблемы с установкой, используйте консольную версию:

```bash
cd /Users/user/Documents/driver-solution-cad
./demo_menu_console
```

**Функциональность:**
- Полное меню со всеми пунктами
- Создание 2D документов
- Работа с файлами
- Все функции GUI версии

---

## 🔍 Решение проблем

### Проблема 1: "Could NOT find wxWidgets"

**Решение:**
```bash
brew install wxwidgets
brew link wxwidgets
```

### Проблема 2: "sudo: a password is required"

**Решение:**
- Введите пароль администратора при запросе
- Или установите в пользовательскую директорию:
```bash
cmake --install . --prefix ~/xtd_install
export CMAKE_PREFIX_PATH=~/xtd_install:$CMAKE_PREFIX_PATH
```

### Проблема 3: "Could NOT find xtd"

**Решение:**
```bash
# Проверьте установку
ls /usr/local/lib/cmake/xtd/xtdConfig.cmake

# Если файл не найден, переустановите:
cd /Users/user/Documents/xtd/build
sudo cmake --install .
```

### Проблема 4: "target_type: Unknown CMake command"

**Решение:**
- Убедитесь, что xtd установлен правильно
- Проверьте версию CMake (нужна 3.20+):
```bash
cmake --version
```

### Проблема 5: Ошибки компиляции GUI

**Решение:**
```bash
# Очистите и пересоберите
cd /Users/user/Documents/driver-solution-cad/build
rm -rf *
cmake ..
cmake --build . --clean-first
```

### Проблема 6: Приложение не запускается

**Решение:**
```bash
# Проверьте зависимости
otool -L build/simple_gui

# Убедитесь, что библиотеки на месте
ls /usr/local/lib/libxtd.*
```

---

## 📝 Быстрая справка команд

### Установка xtd (полный цикл):
```bash
cd /Users/user/Documents/xtd
mkdir -p build && cd build
cmake .. -DXTD_BUILD_TESTS=OFF -DXTD_BUILD_EXAMPLES=OFF
cmake --build . -j$(sysctl -n hw.ncpu)
sudo cmake --install .
```

### Сборка GUI приложения:
```bash
cd /Users/user/Documents/driver-solution-cad
mkdir -p build && cd build
cmake ..
cmake --build .
./simple_gui
```

### Запуск консольной версии:
```bash
cd /Users/user/Documents/driver-solution-cad
./demo_menu_console
```

---

## 📞 Дополнительная информация

- **Документация xtd:** https://gammasoft71.github.io/xtd
- **Репозиторий xtd:** https://github.com/gammasoft71/xtd
- **Файлы проекта:**
  - GUI код: `examples/simple_gui.cpp`
  - Консольное меню: `examples/demo_menu_console.cpp`
  - CMake конфигурация: `CMakeLists.txt`

---

## ✅ Чеклист установки

- [ ] Homebrew установлен
- [ ] wxWidgets установлен (`brew install wxwidgets`)
- [ ] CMake установлен (версия 3.20+)
- [ ] xtd собран (`cmake --build .`)
- [ ] xtd установлен (`sudo cmake --install .`)
- [ ] GUI приложение собрано (`cmake --build .`)
- [ ] GUI приложение запускается (`./simple_gui`)

---

**Удачи с установкой! 🚀**

